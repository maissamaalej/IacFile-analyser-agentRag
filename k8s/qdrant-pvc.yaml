apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agentrag-qdrant-pvc
  namespace: agentrag
spec:
  storageClassName: local-storage

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 20Gi
