apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agentrag-mysql-pvc
  namespace: agentrag
spec:
  storageClassName: local-storage

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 10Gi
