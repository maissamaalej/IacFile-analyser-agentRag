apiVersion: v1
kind: Service
metadata:
  name: agentrag-backend
  namespace: agentrag
spec:
  type: ClusterIP

  selector:
    app: agentrag-backend

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
