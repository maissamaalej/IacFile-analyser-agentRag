apiVersion: v1
kind: Service
metadata:
  name: agentrag-frontend
  namespace: agentrag
spec:
  selector:
    app: agentrag-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP