apiVersion: v1
kind: Service
metadata:
  name: agentrag-frontend
  namespace: agentrag
spec:
  type: ClusterIP

  selector:
    app: agentrag-frontend

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
